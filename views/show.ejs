<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
    integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
    crossorigin="anonymous">
    <title>Secret Information</title>
    <style>
        #wrapper {
            background-color: lightgrey;
            padding-bottom: 30px;
        }
        #top a {
            padding: 15px;
            font-size: 24px;
            margin-left: 300px;
            display: inline-block;
        }
        #main {
            padding: 20px;
        }
        #blueline {
            background-color: blue;
            height: 2px;
            margin-bottom: 15px;
        }
        h2 {
            text-align: center;
        }
        h4 {
            text-align: center;
        }
        table {
            border: 1px solid black;
            border-collapse: collapse;
            margin: 10px;
            margin-top: 50px;
            margin-left: 300px; 
            margin-bottom: 30px;
            width: 800px;
        }
        th {
            text-align: left;
            border: 1px solid black;
            border-collapse: collapse;
            padding: 5px;
            font-size: 25px;
            padding-left: 25px;
            padding-right: 600px;
            background-color: rgb(154, 175, 192);
        }
        tr td {
            border: 1px solid black;
            border-collapse: collapse;
            padding: 10px;
            background-color: rgb(203, 224, 230);
        }
        .error {
            color: rgb(182, 29, 29);
        }
        input[type = submit] {
            font-size: 18px;
            background-color: rgb(80, 155, 77);
            color: white;
            padding: 10px 16px;
            margin: 0;
            border: none;
            border-radius: 8px;
            cursor: pointer;
        }
        #commentform {
            margin-left: 250px;
        }
    </style>
</head>
<body>
    <div id = "wrapper">
        <div id = "top">
            <a href = "/secrets">Back to Secrets Homepage</a>
            <a href = "/logout">Logout!</a>
        </div>
        <div id = "blueline"></div>
        <h2><u>Comment on this secret</u></h2>
        <div id = "main">
            <h4><%= secret.secret %></h4>
            <table>
                <tr>
                    <th>Comments</th>
                </tr>
                <% for (comment of secret.comments) { %>
                <tr>
                    <td><%= comment.comment %></td>
                </tr>
                <% } %>
            </table>
            <div id = "commentform">
                <% if (messages.comment) { %>
                    <% for (var x of messages.comment) { %>
                        <div class = "error"><%= x %></div>
                    <% } %>
                <% } %>
                <form action = "/secrets/<%= secret.id %>/comment" method = "post">
                    <textarea name = "comment" rows = "4" cols = "120"></textarea>
                    <br><input type = "submit" value = "Post a comment!">
                </form>
            </div>
        </div>
    </div>
</body>
</html>